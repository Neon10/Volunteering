@model Volunteering.Domain.Entities.Invitation

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>DeleteInvit</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Invitation</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.ActionId)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.ActionId)
            </dd>
    
            <dt>
                @Html.DisplayNameFor(model => model.VolunteerId)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.VolunteerId)
            </dd>
    
        </dl>
    
        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()
    
            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</body>
</html>
